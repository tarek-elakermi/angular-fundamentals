import { TransformCustomPipePipe } from './transform-custom-pipe.pipe';

describe('TransformCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
