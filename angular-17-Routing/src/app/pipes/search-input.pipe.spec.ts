import { SearchInputPipe } from './search-input.pipe';

describe('SearchInputPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInputPipe();
    expect(pipe).toBeTruthy();
  });
});
