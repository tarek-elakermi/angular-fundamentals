import { UserDirDirective } from './user-dir.directive';

describe('UserDirDirective', () => {
  it('should create an instance', () => {
    const directive = new UserDirDirective();
    expect(directive).toBeTruthy();
  });
});
